#include<stdio.h> 
#include<iostream>

int fb(int num) 
{ 
   if (num <= 1) 
      return num; 
      return fb(num-1) + fb(num-2); 
} 
 using namespace std; 
int main () 
{ 
   
  int n;
  cin>>n;
  cout<<fb(n); 
  return 0; 
} 
